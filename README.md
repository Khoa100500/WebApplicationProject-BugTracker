# WebApplicationProject-BugTracker

Run backend server:

- Install dependencies: npm install
- Run server: npm start

Run frontend server:

- Install dependencies: npm install
- Run server: npm start
